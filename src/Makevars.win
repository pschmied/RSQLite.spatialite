ifndef LIB_ICONV
 LIB_ICONV=.
endif

PKG_CPPFLAGS = -I$(R_HOME)/include -I${LIB_ICONV}/include -I${LIB_ICONV} -I${GEOS_HOME}/include -I$(GDAL_HOME)/include -DVERSION=\"3.0.1\" -D_LARGE_FILE=1 -D_FILE_OFFSET_BITS=64 -D_LARGEFILE_SOURCE=1 -DSQLITE_ENABLE_RTREE=1 -DNDEBUG=1 -DSPL_AMALGAMATION=1\
   -DOMIT_ICONV -DOMIT_FREEXL 
PKG_LIBS = -L$(GDAL_HOME)/lib -lproj -L${GEOS_HOME}/lib -lgeos_c -lgeos -lstdc++
